# Лабораторная работа №3

## Задание:

Child1 переводит строки в верхний регистр. Child2 превращает все пробельные
символы в символ «_».


## Запуск лабораторной:

```bash
gcc server.c -o server
gcc child_1.c -o child_1
gcc child_2.c -o child_2
./server
```
